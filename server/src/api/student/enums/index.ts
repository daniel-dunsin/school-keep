export enum StudentStatus {
  Active = 'active',
  Expelled = 'expelled',
  Suspended = 'suspended',
  Cleared = 'cleared',
}

export enum StudentActivityType {
  Normal = 'normal',
  Warning = 'warning',
  Destructive = 'destructive',
}
