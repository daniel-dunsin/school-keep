import 'package:app/data/clearance/models/student_clearance_status_model.dart';
import 'package:flutter/material.dart';

class ClearanceRejectedWidget extends StatelessWidget {
  final StudentClearanceStatusModel clearanceStatus;
  const ClearanceRejectedWidget({super.key, required this.clearanceStatus});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
